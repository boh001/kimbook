if user                                    
    .chat__users
        .users__friends(id=user.id)
            if users.friends
                each friend in users.friends
                    .friends__list.jsJoinBtn(id=friend.id)
                        .list__info
                            img.miniAvatar2(src=`${friend.avatarUrl}`)
                            .info__nickname=friend.nickname